# Логика управления публикациями 1С (IIS / Webinst)

## Обзор
Функционал управления публикациями перенесен на вкладку **"Клиенты"** для обеспечения единой точки входа управления информационными базами. Ранее этот функционал находился в разделе "Настройки".

Теперь администратор видит статус публикации (Web) прямо в карточке базы данных клиента и может управлять ею "на месте".

## Основные возможности

### 1. Индикация статуса
В списке баз данных (в развернутой карточке клиента) каждая база отображает свой статус публикации:
- **Не опубликована**: Отображается иконка 🌐 (Опубликовать).
- **Опубликована**: Отображается плашка `WEB` с указанием версии платформы (например, `v8.3.24.1342`) и URL-пути. Вместо кнопки публикации отображается иконка ⚙️ (Настройки).

### 2. Создание публикации
При нажатии на иконку 🌐 открывается модальное окно:
- **Имя публикации (URL path)**: По умолчанию совпадает с именем базы.
- **Путь к папке**: Физический путь (по умолчанию `C:\inetpub\wwwroot\BaseName`).
- **Версия платформы**: Выбирается из установленных (автоматически подставляется версия по умолчанию из настроек).

При подтверждении отправляется команда `Publish` агенту, который выполняет `webinst`.

### 3. Редактирование публикации
При нажатии на иконку ⚙️ (для уже опубликованной базы) открывается то же модальное окно, но с предзаполненными данными текущей публикации.
Это позволяет:
- Изменить версию платформы для конкретной базы.
- Изменить физический путь.
- (Примечание: Изменение имени публикации может привести к созданию новой публикации, если IIS не поддерживает переименование "на лету" через текущую реализацию команды).

### 4. Массовая смена платформы
В заголовке вкладки "Клиенты" доступна кнопка **"🔄 Массовая смена платформы"**.
Позволяет выбрать:
- **Исходную версию** (Source)
- **Целевую версию** (Target)

Команда `MassUpdateVersions` отправляется агенту, который находит все публикации на *Исходной* версии и перепубликует их на *Целевой*, сохраняя пути и настройки.

## Техническая реализация

### UI
- **ClientsPage.tsx**: Содержит состояние модальных окон (`isPubModalOpen`, `isMassModalOpen`) и логику отправки команд.
- **ClientRow.tsx**: Отвечает за рендеринг карточки базы и сопоставление базы с публикацией. Сопоставление происходит по атрибуту `AppPath` публикации и имени базы.
- **ClientTable.tsx**: Пробрасывает колбэки от страницы к строкам.

### Взаимодействие с Backend (Agent)
Используется API `/api/agents/{id}/commands`.

**Команда `Publish`**:
```json
{
  "Type": "Publish",
  "PayloadJson": {
    "BaseName": "buh_base",
    "FolderPath": "C:\\inetpub\\wwwroot\\buh_base",
    "ConnectionString": "Srvr=\"localhost\";Ref=\"buh_base\";",
    "Version": "8.3.24.1342"
  }
}
```

**Команда `MassUpdateVersions`**:
```json
{
  "Type": "MassUpdateVersions",
  "PayloadJson": {
    "SourceVersion": "8.3.23.1000",
    "TargetVersion": "8.3.24.1342"
  }
}
```

## Настройки
В глобальных настройках (`SettingsPage` -> `OneCSection`) остался только выбор **Версии платформы по умолчанию**. Эта настройка используется как предустановка для новых публикаций.
