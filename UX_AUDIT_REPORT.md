# UX/UI АУДИТ: 1C Session Manager
**Режим:** Read-Only Analysis  
**Дата:** 2025  
**Аудитор:** Senior UX/UI Auditor (Enterprise B2B Admin Systems)

---

## 1. СИСТЕМНЫЕ UX ПРОБЛЕМЫ

> **Статус внедрения (обновляется по мере исправлений):**  
> ✅ 2025-12-15 — Внедрены toast-уведомления вместо `alert()`, добавлены подтверждения для опасных действий на “Клиенты”, сохранение фильтров/сортировки “Клиенты” в URL, сортировка кликом по заголовкам, индикатор “обновлено … назад”, детали события переведены из модалки в правый drawer.  

### 1.1. Критические проблемы когнитивной нагрузки

**Проблема:** Страница "Клиенты" перегружена функциональностью и визуальной информацией.

**Детали:**
- На одной странице объединены: управление клиентами, управление базами данных, публикации IIS, массовые операции, фильтрация, сортировка, статистика
- В развернутом виде клиента показывается сетка из 4 колонок баз данных с дополнительными действиями
- Модальные окна вложены друг в друга (ClientModal → DatabaseSelection → поиск)
- Три независимых модальных окна на странице (ClientModal, MassUpdate, Publish)

**Влияние на админа:**
- Высокий порог входа для новых пользователей
- Риск ошибок при выполнении операций (особенно публикации)
- Сложность обучения новых сотрудников
- Медленное выполнение задач из-за необходимости переключения контекста

### 1.2. Непоследовательная обработка ошибок

**Проблема:** Использование `alert()` для критических сообщений вместо встроенной системы уведомлений.

**Статус:** ✅ **Исправлено (2025-12-15)** — `alert()` заменены на единые toast-уведомления.  
**Реализация:** `ui/components/ui/toast/*`, `ui/hooks/useToast.ts`, подключение в `ui/index.tsx`.  
**Проверка:** в `ui/` больше нет `alert(`.  

**Примеры:**
```12:14:ui/pages/clients/ClientsPage.tsx
alert('Команда отправлена агенту. Статус обновится через минуту.');
```

```232:233:ui/pages/clients/ClientsPage.tsx
alert('База уже привязана к этому клиенту');
```

**Влияние:**
- Нарушает UX flow (блокирующие диалоги)
- Нет истории ошибок
- Невозможно скопировать текст ошибки
- Плохо для скриншотов/документации

### 1.3. Отсутствие визуальной иерархии действий

**Проблема:** Все действия выглядят одинаково важными.

**Статус:** ✅/🟡 **Частично исправлено (2025-12-15)** — в списке клиентов “Удалить” убрано в меню действий (…), “События” оставлено быстрым действием, “Редактировать” доступно из меню и из Drawer деталей клиента. В `ClientDetailsDrawer` внутри карточек инфобаз основное действие сделано явным (кнопка “Опубликовать/Публикация”), а опасная “Отвязать” перенесена в меню действий. Осталось: выровнять иерархию на других экранах (например, крупные вторичные действия рядом с “Добавить”).

**Примеры:**
- Кнопка "Массовая смена платформы" (вторичное действие) находится рядом с "Добавить клиента" (первичное)
- В ClientRow кнопки "События", "Редактировать", "Удалить" имеют одинаковый визуальный вес
- В развернутом виде базы данных кнопки "Отвязать", "Опубликовать", "Настройки" не различаются по важности

**Влияние:**
- Админ не понимает, что делать в первую очередь
- Риск случайных кликов на опасные действия
- Нет четкого workflow

### 1.4. Проблемы с состоянием загрузки

**Проблема:** Несогласованная обработка состояний загрузки.

**Примеры:**
- `useClients` обновляет данные, но важно явно показывать “обновление…” и время последнего апдейта
- Долгие агентские операции должны иметь понятный статус/прогресс/ETA
- В `DatabaseSelection` есть спиннер, но он не связан с общим состоянием страницы

**Статус:** ✅/🟡 **Частично исправлено (2025-12-15)**  
- ✅ Добавлен индикатор актуальности/фонового обновления клиентов (`lastUpdate`, `isRefreshing`) и ручное “Обновить”.  
- ✅ Для “Массовая смена платформы” добавлен предпросмотр (сколько публикаций затронется) + подтверждение.  
- ✅ Добавлен прогресс/ETA для долгих агентских операций через “Очередь агента” (progress percent/message/ETA).  

**Влияние:**
- Админ не знает, когда данные актуальны
- Возможны конфликты при одновременном редактировании
- Нет обратной связи при долгих операциях

---

## 2. НАВИГАЦИЯ И ИНФОРМАЦИОННАЯ АРХИТЕКТУРА

### 2.1. Неочевидная маршрутизация

**Проблема:** Использование hash-based routing (`#/status`, `#/clients`) без визуальных индикаторов.

**Статус:** ✅/🟡 **Частично исправлено (2025-12-15)** — добавлен deep‑link на “Детали клиента”: `#/clients?...&clientId=...` автоматически открывает Drawer нужного клиента; в Drawer добавлена кнопка копирования ссылки. Добавлен возврат контекста “Клиенты → События” через `return=` и кнопка “Назад” в “События”. ✅ Улучшены breadcrumbs: реактивны к `hashchange`, показывают контекст вкладок/режимов и добавлена универсальная кнопка “Назад” (через `return=` или историю hash‑навигации). ✅ Добавлен вложенный маршрут для массовых операций: `#/clients/publications/mass-update`. Полные вложенные маршруты для остальных сущностей/операций пока не внедрены.

**Детали:**
```13:26:ui/App.tsx
function getRouteFromHash(): RouteId {
  const raw = (window.location.hash || '').replace(/^#/, '');
  const path = raw.startsWith('/') ? raw.slice(1) : raw;
  const route = (path.split('?')[0] || '').trim();
  switch (route) {
    case 'events':
    case 'clients':
    case 'settings':
    case 'status':
      return route;
    default:
      return 'status';
  }
}
```

**Проблемы:**
- Нет breadcrumbs
- Невозможно открыть ссылку в новой вкладке с контекстом
- Сложно поделиться ссылкой на конкретного клиента
- Нет истории навигации (кнопка "Назад")

**Влияние:**
- Админы теряются при переходах между страницами
- Невозможно вернуться к предыдущему состоянию
- Сложно работать в нескольких вкладках

### 2.2. Несогласованность терминологии

**Проблема:** Смешение терминов в интерфейсе.

**Статус:** ✅ **Исправлено (2025-12-15)** — унифицированы термины в UI: “Клиент” (вместо “Организация” в форме), “Инфобаза” (вместо “База данных/БД” в деталях событий/клиентов), “Хранилище (SQL)” для SQL‑интеграции (вместо “DB endpoint/SQL login”). Проведена финальная вычитка ключевых экранов (Dashboard/Settings/Clients/Events), убраны англоязычные хвосты в микрокопи.

**Примеры:**
- "Клиенты" vs "Организации" (в ClientModal: "Название организации")
- "Инфобазы" vs "Базы данных" (используются оба термина)
- "Сеансы" vs "Сессии" (в коде `activeSessions`, в UI "Сеансы")
- "Публикация" vs "WEB публикация" (неясно, что это IIS)

**Влияние:**
- Путаница у новых пользователей
- Сложность документирования
- Непрофессиональный вид

### 2.3. Отсутствие глобального поиска

**Проблема:** Нет возможности найти клиента/базу/событие из любого места интерфейса.

**Статус:** ✅/🟡 **Частично исправлено (2025-12-15)** — добавлен глобальный поиск (Command Palette) по **Ctrl+K**: поиск клиентов и инфобаз + быстрые переходы. ✅ Добавлен поиск по событиям/сообщениям (warn+): при открытии палитры подгружаются последние события за текущий time range и доступны в выдаче по тексту/контексту. ✅ Добавлены пользовательские **saved views** для “События” (локально, через фильтр‑бар). ✅ Добавлены “saved queries” для Command Palette в виде **сохранённых ссылок** (pin текущего `#/...` состояния с фильтрами/вкладками) + управление/удаление.

**Текущее состояние:**
- Поиск только на странице "Клиенты"
- На странице "События" есть фильтры, но нет глобального поиска
- Нет быстрого доступа (Cmd+K / Ctrl+K)

**Влияние:**
- Медленный доступ к нужной информации
- Необходимость помнить, на какой странице что искать

### 2.4. Проблемы с боковой панелью

**Проблема:** Sidebar не показывает контекстную информацию.

**Статус:** ✅/🟡 **Частично исправлено (2025-12-15)** — добавлены бейджи в Sidebar:  
- “События”: количество critical (или warning, если critical=0) за текущий time range  
- “Клиенты”: количество клиентов в блокировке/на лимите (или “в риске”, если критичных нет)  
- “Настройки”: количество незаполненных пунктов setup (DB/SQL/API key)  
✅ Добавлен индикатор “+N новых” для “События” (новые с последнего посещения).  
✅ Добавлены быстрые shortcuts в Sidebar: “Поиск (Ctrl+K)”, “События (warn+)”, “Клиенты (перелимит)”, “Клиенты (в риске)”.  

**Детали:**
```21:26:ui/components/Sidebar.tsx
const menuItems: Array<{ id: string; label: string; icon: any; hint?: string }> = [
  { id: 'status', label: 'Обзор', icon: Gauge, hint: 'Состояние и активные проблемы' },
  { id: 'events', label: 'События', icon: FileText, hint: 'Логи, алерты, расследование' },
  { id: 'clients', label: 'Клиенты', icon: Users, hint: 'Квоты, инфобазы, лимиты' },
  { id: 'settings', label: 'Настройки', icon: SettingsIcon, hint: 'Интеграции и политика' },
];
```

**Что отсутствует:**
- Бейджи с количеством проблем/предупреждений
- Индикаторы новых событий
- Быстрые действия (shortcuts)

**Влияние:**
- Админ не видит критичность проблем без перехода на страницу
- Нет мотивации к действию

---

## 3. ПОСТРАНИЧНЫЙ АНАЛИЗ

### 3.1. Dashboard (Обзор)

**Сильные стороны:**
- Четкая иерархия: Health → Issues → Stats
- Использование визуальных индикаторов (HealthStatus)
- Группировка информации по важности

**Проблемы:**
- Нет быстрых действий (quick actions)
- Статистика не кликабельна (нельзя перейти к деталям)
- Нет настройки виджетов (админ не может скрыть ненужное)

**Статус:** ✅/🟡 **Частично исправлено (2025-12-15)** — добавлен блок “Быстрые действия”; сделаны кликабельными ключевые карточки “Snapshot” (переходы в События/Клиенты/Настройки); добавлен click-through из виджетов: “Клиенты под нагрузкой” → `#/clients?...&clientId=...` (с открытием Drawer) и “Крупнейшие инфобазы” → `#/events?...&database=...`. Кастомизация виджетов пока не реализована.

### 3.2. Events (События)

**Сильные стороны:**
- Хорошая система фильтров
- Автообновление с возможностью отключения
- Детальный просмотр события

**Проблемы:**
 - Нет группировки событий (по клиенту, по базе, по времени)
 - Нет экспорта событий
- Нет возможности отметить событие как "прочитанное"
- Фильтр по клиенту требует знания ID клиента
 - Нет быстрых представлений (saved views) / пресетов фильтров

**Статус:** ✅/🟡 **Частично исправлено (2025-12-15)** — добавлены “быстрые представления” (пресеты) в фильтры событий: All/Critical/Warning/Кластер/SQL; добавлен экспорт (CSV/JSON); добавлено действие “Отметить прочитанными” + пометка NEW в таблице (на основе `lastSeenUtc`); добавлен режим **“Группы”** (по клиенту/инфобазе) с переходом в отфильтрованный список; фильтр клиента стал **по имени (typeahead)**. ✅ Добавлены пользовательские сохранённые представления (Saved Views) для фильтров/режима (локально, через localStorage). Группировка по времени пока не реализована.
✅ **Исправлено (2025-12-15)** — добавлена группировка **по времени** (тайм‑слоты с умным шагом 5м/10м/1ч/6ч/1д/1н) + клик по группе применяет локальный time‑bucket (в рамках текущего time range) и сохраняет его в URL (`bucketFrom/bucketTo`) для шаринга/возврата.

### 3.3. Settings (Настройки)

**Сильные стороны:**
- Логичная группировка по разделам
- Визуальные индикаторы статуса (бейджи DB/SQL/Key)
- Тестирование подключений

**Проблемы:**
- Нет валидации перед сохранением
- Нет отмены изменений (все изменения сразу применяются)
- Секция "UI" минимальна (только тема, которая всегда dark)

**Статус:** ✅/🟡 **Частично исправлено (2025-12-15)** — добавлен draft/dirty-state для 1C/Policy: индикатор “есть несохранённые изменения” + кнопки “Сохранить/Отменить” (reset к последней загруженной конфигурации). Добавлена базовая валидация перед сохранением (RAS host, интервал, rac.exe, пользователь кластера) и показ ошибок у полей. ✅ Улучшен UX `SetupSection`: добавлен чеклист “что настроить”, единая микрокопия/статусы, валидация полей и понятные результаты действий (toast + цветные сообщения).
✅ Расширена секция UI: локальные предпочтения (табличная плотность, формат времени в событиях, автообновление по умолчанию) сохраняются в браузере и применяются в Clients/Events.

### 3.4. Clients (Клиенты) — ДЕТАЛЬНЫЙ РАЗБОР НИЖЕ

---

## 4. ГЛУБОКИЙ АНАЛИЗ: СТРАНИЦА "КЛИЕНТЫ"

### 4.1. Архитектурные проблемы

#### 4.1.1. Монолитная страница

**Проблема:** Одна страница пытается решить слишком много задач.

**Текущая функциональность:**
1. Управление клиентами (CRUD)
2. Управление базами данных (привязка/отвязка)
3. Управление публикациями IIS
4. Массовые операции (смена платформы)
5. Фильтрация и поиск
6. Статистика
7. Работа с нераспределенными базами

**Рекомендация:** Разделить на:
- Основная страница: список клиентов + быстрые действия
- Детальная страница клиента: все базы, публикации, история
- Отдельная страница/модал для массовых операций

**Статус:** ✅/🟡 **Частично исправлено (2025-12-15)** — внедрено MVP‑разделение “Клиенты / Инфобазы / Публикации” как **под‑маршруты**: `#/clients`, `#/clients/infobases`, `#/clients/publications` (с сохранением фильтров в query). ✅ “Массовая смена платформы” вынесена в отдельный экран: `#/clients/publications/mass-update` (deep‑link + Back/Forward). Полное разделение на отдельные страницы/маршруты для остальных операций/сущностей пока не сделано.

#### 4.1.2. Скрытая функциональность

**Проблема:** Критически важные действия скрыты в развернутом виде строки.

**Статус:** ✅ **Исправлено (2025-12-15)** — критичные действия (управление инфобазами/публикациями) вынесены в явный Drawer “Детали клиента”, а не спрятаны в раскрытии строки таблицы.

**Пример:**
```154:264:ui/pages/clients/ClientRow.tsx
{isExpanded && (
  <tr className="bg-indigo-500/5 border-b border-white/10">
    <td colSpan={6} className="px-6 py-6">
      <div className="ml-4">
        {/* Все базы данных с действиями */}
      </div>
    </tr>
)}
```

**Что скрыто:**
- Управление публикациями (критично для работы)
- Отвязка баз (опасное действие)
- Статистика по базам

**Влияние:**
- Админы не знают о существовании функций
- Нужно кликать на каждую строку, чтобы увидеть возможности
- Нет обзора всех публикаций

### 4.2. Проблемы модальных окон

#### 4.2.1. ClientModal: Двухвкладочная структура

**Проблема:** Вкладки "Основная информация" и "Базы данных" создают дополнительный шаг.

```120:141:ui/pages/clients/ClientModal.tsx
<div className="flex border-b mb-6 border-white/10 bg-transparent">
  <button onClick={() => setModalTab('info')}>Основная информация</button>
  <button onClick={() => setModalTab('databases')}>Базы данных</button>
</div>
```

**Проблемы:**
- При создании клиента нужно переключаться между вкладками
- Нет валидации между вкладками
- Можно сохранить клиента без баз (хотя это может быть намеренно)

**Влияние:**
- Лишние клики
- Риск забыть заполнить вкладку "Базы данных"

**Статус:** ✅ **Исправлено (2025-12-15)** — `ClientModal` переведен в “одностраничный” поток: данные клиента + блок выбора инфобаз в одном экране (без вкладок). Это снижает количество кликов и убирает риск “я сохранил клиента, но не заметил вкладку с инфобазами”.

#### 4.2.2. DatabaseSelection: Двойной интерфейс

**Проблема:** Два способа выбора баз: список с чекбоксами И текстовое поле.

```123:137:ui/pages/clients/DatabaseSelection.tsx
<div className="mt-3 pt-3 border-t border-white/10">
  <label className="block text-xs font-semibold mb-1 text-slate-200">
    Список баз данных (вручную):
  </label>
  <textarea 
    value={manualDbInput}
    onChange={e => setManualDbInput(e.target.value)}
    placeholder="Base1, Base2, Base3..."
    rows={3}
  />
</div>
```

**Проблемы:**
- Неясно, какой способ использовать
- Текстовое поле не синхронизируется с чекбоксами визуально
- Можно ввести несуществующую базу вручную (валидация только при сохранении)

**Влияние:**
- Путаница у пользователей
- Риск ошибок (опечатки в названиях)

**Статус:** ✅ **Исправлено (2025-12-15)** — убран параллельный ввод через textarea. Теперь один источник истины: выбранные инфобазы (список + чипы) + поиск по агентскому списку; “ручное добавление” работает через одно поле ввода и добавляет только существующие инфобазы (с предупреждением, если не найдено/есть конфликт привязки).

#### 4.2.3. Publish Modal: Недостаточная информация

**Проблема:** Модальное окно публикации не показывает текущее состояние.

```420:446:ui/pages/clients/ClientsPage.tsx
<Modal
  isOpen={isPubModalOpen}
  onClose={() => setIsPubModalOpen(false)}
  title={isEditingPub ? `Настройки публикации: ${pubDbName}` : `Новая публикация: ${pubDbName}`}
>
  <div className="space-y-4">
    <Input label="Имя публикации (URL path)" value={pubName} onChange={e => setPubName(e.target.value)} />
    <Input label="Путь к папке (Physical Path)" value={pubPath} onChange={e => setPubPath(e.target.value)} />
    <Select label="Версия платформы" value={pubVer} onChange={e => setPubVer(e.target.value)} options={...} />
  </div>
</Modal>
```

**Что отсутствует:**
- Текущие настройки публикации (при редактировании)
- Предупреждения о конфликтах
- Справка по форматам путей
- Валидация путей

**Влияние:**
- Риск ошибок при публикации
- Невозможно понять, что изменилось

**Статус:** ✅/🟡 **Частично исправлено (2025-12-15)** — модалка публикации показывает “текущее состояние (обнаружено агентом)” при наличии публикации (IIS‑сайт/URL/путь/версия/время обнаружения), добавлено подтверждение перед отправкой команды и базовая валидация имени публикации. Осталось (по желанию): подсказки/валидация форматов путей, предупреждения о конфликтах IIS‑сайта/URL, “diff” изменений при редактировании.

### 4.3. Проблемы таблицы

#### 4.3.1. Неочевидная интерактивность

**Проблема:** Строки таблицы выглядят статичными, но раскрываются по клику.

**Статус:** ✅ **Исправлено (2025-12-15)** — убрано “скрытое раскрытие” строк. Вместо этого: явная кнопка “Детали” + клик по строке открывает Drawer с деталями.

```51:152:ui/pages/clients/ClientRow.tsx
<tr className={`group transition-colors ${isExpanded ? 'bg-indigo-500/10' : 'hover:bg-white/5'}`}>
  <td className="px-6 py-4">
    <button onClick={onToggleExpand}>
      {isExpanded ? <ChevronUp /> : <ChevronDown />}
    </button>
  </td>
  {/* ... */}
</tr>
```

**Проблемы:**
- Нет визуального указания, что строку можно раскрыть
- Иконка ChevronDown маленькая и не бросается в глаза
- Нет подсказки при наведении

**Влияние:**
- Админы не понимают, что можно раскрыть строку
- Нужно кликать наугад

#### 4.3.2. Перегруженная колонка "Инфобазы"

**Проблема:** В колонке показываются только 2 базы + счетчик остальных.

```46:90:ui/pages/clients/ClientRow.tsx
const previewDbs = client.databases.slice(0, 2);
const remainingDbs = client.databases.length - previewDbs.length;

{previewDbs.map((db, idx) => (
  <span className="...">{db.name}</span>
))}
{remainingDbs > 0 && (
  <button onClick={onToggleExpand}>+{remainingDbs} еще</button>
)}
```

**Проблемы:**
- Не видно все базы сразу
- Нужно раскрывать строку для полной информации
- Счетчик "+N еще" выглядит как кнопка, но не очевидно

**Влияние:**
- Невозможно быстро оценить количество баз у клиента
- Лишние клики для просмотра

#### 4.3.3. Колонка "Сеансы": Недостаточная информация

**Проблема:** Показывается только факт/план и процент, но нет контекста.

```92:118:ui/pages/clients/ClientRow.tsx
<div className="w-full max-w-[180px]">
  <div className="flex justify-between text-sm mb-2">
    <span>{client.activeSessions} / {isUnlimited ? '∞' : client.maxSessions}</span>
    {!isUnlimited && <span>{percentage}%</span>}
  </div>
  <div className="w-full h-3 rounded-full overflow-hidden bg-white/10">
    {/* Progress bar */}
  </div>
</div>
```

**Что отсутствует:**
- Тренд (растет/падает)
- Время последнего обновления
- Разбивка по базам (без раскрытия)

**Влияние:**
- Невозможно быстро понять динамику
- Нужно раскрывать строку для деталей

### 4.4. Проблемы фильтрации и поиска

#### 4.4.1. Несохраняемые фильтры

**Проблема:** Фильтры сбрасываются при перезагрузке страницы.

```28:32:ui/pages/clients/ClientsPage.tsx
const [searchTerm, setSearchTerm] = useState('');
const [statusFilter, setStatusFilter] = useState<StatusFilter>('all');
const [limitFilter, setLimitFilter] = useState<LimitFilter>('all');
const [sortBy, setSortBy] = useState<'name' | 'sessions' | 'databases' | 'status'>('name');
const [sortOrder, setSortOrder] = useState<'asc' | 'desc'>('asc');
```

**Влияние:**
- Админ должен заново настраивать фильтры после перезагрузки
- Невозможно поделиться ссылкой с примененными фильтрами

#### 4.4.2. Недостаточные фильтры

**Проблема:** Нет фильтров по критическим параметрам.

**Отсутствуют:**
- Фильтр по количеству сеансов (например, "> 80% лимита")
- Фильтр по наличию публикаций
- Фильтр по количеству баз
- Фильтр по дате последнего обновления

**Влияние:**
- Невозможно быстро найти проблемные клиенты
- Нужно просматривать всех вручную

#### 4.4.3. Проблемы с сортировкой

**Проблема:** Сортировка скрыта в нижней части карточки фильтров.

```343:365:ui/pages/clients/ClientsPage.tsx
<div className="mt-3 flex items-center justify-between text-xs text-slate-400">
  <span>Найдено клиентов: <b>{filteredAndSortedClients.length}</b></span>
  <div className="flex items-center gap-2">
    <span>Сортировка:</span>
    <select value={sortBy} onChange={e => setSortBy(e.target.value as any)}>
      {/* options */}
    </select>
    <button onClick={() => setSortOrder(...)}>
      <ArrowUpDown size={14} />
    </button>
  </div>
</div>
```

**Проблемы:**
- Неочевидное расположение
- Нет визуального указания текущей сортировки в заголовках колонок
- Кнопка ArrowUpDown не показывает направление

**Влияние:**
- Админы не замечают возможность сортировки
- Непонятно, по какой колонке идет сортировка

### 4.5. Проблемы с нераспределенными базами

#### 4.5.1. Неочевидное расположение

**Проблема:** Блок "Нераспределенные базы" находится в середине страницы, после статистики.

```283:289:ui/pages/clients/ClientsPage.tsx
<UnassignedDatabases 
  unassignedDatabases={unassignedDatabases}
  clients={clients}
  onAssign={handleQuickAssign}
  onRefresh={fetchDatabases}
  loading={loadingDbs}
/>
```

**Проблемы:**
- Критически важная информация (нераспределенные базы) не бросается в глаза
- Если баз много, блок занимает много места и отталкивает таблицу вниз
- Нет возможности скрыть блок, если он не нужен

**Влияние:**
- Админы могут пропустить нераспределенные базы
- Страница становится длинной и неудобной

**Статус:** ✅ **Исправлено (2025-12-15)** — операционные задачи по инфобазам вынесены в отдельный под‑экран `#/clients/infobases`, чтобы не “растягивать” основной список клиентов и не прятать важный блок в середине страницы.

#### 4.5.2. Неудобное присвоение

**Проблема:** Присвоение базы требует выбора из выпадающего списка, который появляется на месте кнопки.

```52:74:ui/pages/clients/UnassignedDatabases.tsx
{assigningDb === db.name ? (
  <select
    className="..."
    onChange={(e) => {
      if (e.target.value) {
        onAssign(db.name, e.target.value);
      }
    }}
    onBlur={() => setAssigningDb(null)}
    autoFocus
  >
    <option value="">Выберите клиента...</option>
    {/* options */}
  </select>
) : (
  <button onClick={() => setAssigningDb(db.name)}>Присвоить</button>
)}
```

**Проблемы:**
- Выпадающий список появляется неожиданно
- При потере фокуса (onBlur) список закрывается, что может быть случайным
- Нет поиска в списке клиентов (если их много)

**Влияние:**
- Неудобно работать с большим количеством клиентов
- Риск случайного закрытия

**Статус:** ✅ **Исправлено (2025-12-15)** — присвоение сделано предсказуемым: кнопка “Присвоить” раскрывает поиск по клиентам + выбор из списка, добавлена явная “Отмена”, убран `onBlur`‑автозакрытие.

### 4.6. Проблемы с действиями

#### 4.6.1. Опасные действия без подтверждения

**Проблема:** Удаление клиента требует подтверждения, но отвязка базы — нет.

```212:225:ui/pages/clients/ClientsPage.tsx
const handleRemoveDatabase = (clientId: string, dbName: string) => {
  const client = clients.find(c => c.id === clientId);
  if (!client) return;
  
  const updatedClient = {
    ...client,
    databases: client.databases.filter(db => db.name !== dbName),
    // ...
  };
  
  onUpdate(updatedClient);
};
```

**Проблемы:**
- Отвязка базы — необратимое действие (или требует ручного восстановления)
- Нет подтверждения
- Кнопка маленькая и легко нажимается случайно

**Влияние:**
- Риск случайной потери данных
- Нет возможности отменить действие

#### 4.6.2. Неочевидные действия

**Проблема:** Действия скрыты за иконками без текстовых подсказок в некоторых местах.

**Примеры:**
- Кнопка "События" (FileText) — только иконка
- Кнопки в развернутом виде базы (XCircle, Globe, Settings) — только иконки
- Кнопка сортировки (ArrowUpDown) — только иконка

**Влияние:**
- Нужно наводить курсор для tooltip
- Медленнее работа
- Недоступно для пользователей с ограниченными возможностями

#### 4.6.3. Массовые операции без предпросмотра

**Проблема:** "Массовая смена платформы" не показывает, какие публикации будут затронуты.

**Статус:** ✅ **Исправлено (2025-12-15)** — добавлен предпросмотр “Публикаций к изменению” + подтверждение перед запуском. Операция вынесена в отдельный экран `#/clients/publications/mass-update` (не модалка), что улучшает навигацию и расследование через историю/ссылки.  

```text
Маршрут: #/clients/publications/mass-update
Экран показывает: версии (откуда/куда), предпросмотр (сколько публикаций затронется), и требует подтверждение перед запуском.
```

**Что отсутствует:**
- Список публикаций, которые будут изменены
- Предупреждение о возможных последствиях
- Возможность исключить некоторые публикации

**Влияние:**
- Риск неожиданных изменений
- Невозможно проверить перед выполнением

---

## 5. ОЦЕНКА РИСКОВ И ПРИОРИТЕТОВ

### 🔴 КРИТИЧЕСКИЙ ПРИОРИТЕТ (P0)

1. **Опасные действия без подтверждения**
   - Риск: Потеря данных, необратимые изменения
   - Затронуто: Отвязка баз, массовые операции
   - Время на исправление: 2-4 часа  
   - **Статус:** ✅ **Исправлено (2025-12-15)** — подтверждения добавлены для отвязки инфобазы, публикации и массовой смены платформы. Удаление клиента переведено на единый ConfirmDialog.

2. **Отсутствие обратной связи при операциях**
   - Риск: Админ не знает, выполнена ли операция
   - Затронуто: Все модальные окна, массовые операции
   - Время на исправление: 4-8 часов  
   - **Статус:** ✅/🟡 **Частично исправлено (2025-12-15)** — внедрены toast-уведомления (успех/ошибка/предупреждение). Добавлен UX статусов агентских команд: эндпоинт `GET /api/agents/{agentId}/commands` + “Очередь агента” во вкладке “Клиенты → Публикации” с автообновлением. ✅ Добавлен прогресс команд (percent/message/ETA) для долгих операций: расширена модель `AgentCommand` (progress fields + timestamps), агент обновляет прогресс во время `MassUpdateVersions` (и базово для Publish/Update), UI показывает прогресс‑бар и ETA по данным команды. Полный “пошаговый” прогресс для всех типов команд и единый словарь типов/стадий — можно улучшить отдельно.

3. **Перегруженная страница "Клиенты"**
   - Риск: Ошибки пользователей, низкая продуктивность
   - Затронуто: Вся страница
   - Время на исправление: 2-3 дня (рефакторинг)  
   - **Статус:** ✅/🟡 **Частично исправлено (2025-12-15)** — сделаны точечные улучшения (сортировка, URL‑состояние, подтверждения) + внедрено MVP‑разделение `#/clients` на под‑маршруты: `#/clients`, `#/clients/infobases`, `#/clients/publications`. Полный рефакторинг в отдельные страницы/маршруты (и отдельные экраны для массовых операций) пока не выполнен.

### 🟠 ВЫСОКИЙ ПРИОРИТЕТ (P1)

1. **Несохраняемые фильтры и состояние**
   - Риск: Раздражение пользователей, потеря времени
   - Затронуто: Страница "Клиенты", "События"
   - Время на исправление: 1 день  
   - **Статус:** ✅ **Исправлено (2025-12-15)** — фильтры/сортировка “Клиенты” сохраняются в `#/clients?...` (hash query).

2. **Скрытая функциональность (раскрывающиеся строки)**
   - Риск: Функции не используются
   - Затронуто: Таблица клиентов
   - Время на исправление: 1 день  
   - **Статус:** ✅ **Исправлено (2025-12-15)** — убран “expanded row”; детали вынесены в `ClientDetailsDrawer` (deep-link `clientId=...`), критичные действия убраны из “раскрытия” и сгруппированы (ActionMenu/Drawer).

3. **Использование alert() вместо системы уведомлений**
   - Риск: Плохой UX, блокирующие диалоги
   - Затронуто: Все страницы
   - Время на исправление: 2-3 дня  
   - **Статус:** ✅ **Исправлено (2025-12-15)** — `alert()` заменены на toast-уведомления.

### 🟡 СРЕДНИЙ ПРИОРИТЕТ (P2)

1. **Отсутствие глобального поиска**
   - Риск: Медленный доступ к информации
   - Затронуто: Вся система
   - Время на исправление: 3-5 дней

2. **Недостаточные фильтры**
   - Риск: Невозможно быстро найти проблемные клиенты
   - Затронуто: Страница "Клиенты"
   - Время на исправление: 2-3 дня  
   - **Статус:** ✅ **Исправлено (2025-12-15)** — добавлены быстрые операционные фильтры на “Клиенты”: “В риске ≥80%”, “Перелимит”, “Без инфобаз”, “Без публикаций”, состояние сохраняется в URL (`ops=...`). ✅ Добавлены пользовательские Saved Views для “Клиенты” (локально): сохранение вкладки + фильтров/сортировки и быстрое применение. ✅ Добавлены фильтры на “Клиенты → Публикации” по версии платформы и IIS‑сайту + поиск по URL/пути (с сохранением в URL).

3. **Проблемы с терминологией**
   - Риск: Путаница у пользователей
   - Затронуто: Весь интерфейс
   - Время на исправление: 1-2 дня (ревизия текстов)

### 🟢 НИЗКИЙ ПРИОРИТЕТ (P3)

1. **Отсутствие breadcrumbs**
   - Риск: Потеря контекста навигации
   - Затронуто: Вся система
   - Время на исправление: 2-3 дня  
   - **Статус:** ✅/🟡 **Частично исправлено (2025-12-15)** — breadcrumbs стали более “enterprise”: реактивны к `hashchange`, показывают контекст вкладки (Клиенты/Инфобазы/Публикации), режим “События → Группы”, и добавлена универсальная кнопка “Назад” (через `return=` или историю hash‑навигации). Полные breadcrumbs для вложенных сущностей (например, публикация/массовые операции как отдельные маршруты) пока не внедрены.

2. **Минимальная секция UI в настройках**
   - Риск: Нет кастомизации
   - Затронуто: Страница "Настройки"
   - Время на исправление: 1-2 дня
   - **Статус:** ✅ **Исправлено (2025-12-15)** — добавлены UI preferences (локально): плотность таблиц (Clients/Events), формат времени в событиях (absolute/relative), автообновление по умолчанию (Events Live).

3. **Отсутствие экспорта данных**
   - Риск: Невозможно создать отчеты
   - Затронуто: Страницы "Клиенты", "События"
   - Время на исправление: 3-5 дней
   - **Статус:** ✅ **Исправлено (2025-12-15)** — добавлен экспорт CSV/JSON: “События” (фильтрованный список) и “Клиенты” (текущий отфильтрованный/отсортированный список).

---

## 6. ВЫВОДЫ АУДИТА

### 6.1. Ключевые проблемы

1. **Когнитивная перегрузка:** Страница "Клиенты" пытается решить слишком много задач одновременно. Это основная проблема системы.

2. **Отсутствие системы обратной связи:** Нет единой системы уведомлений, состояний загрузки, подтверждений действий.

3. **Скрытая функциональность:** Критически важные действия (публикации, управление базами) скрыты в раскрывающихся строках.

4. **Небезопасные паттерны:** Опасные действия выполняются без подтверждения или с недостаточным предупреждением.

5. **Плохая навигация:** Hash-based routing без визуальных индикаторов, отсутствие breadcrumbs, невозможность поделиться ссылками.

### 6.2. Сильные стороны

1. **Визуальный дизайн:** Современный dark theme, хорошее использование цветов для статусов.

2. **Структура Dashboard:** Логичная иерархия информации, фокус на важном.

3. **Система фильтров Events:** Хорошо продуманная фильтрация событий.

4. **Модульность компонентов:** Код хорошо структурирован, компоненты переиспользуемы.

### 6.3. Рекомендации по приоритетам

**Фаза 1 (Критично, 1-2 недели):**
- Добавить подтверждения для опасных действий
- Внедрить систему уведомлений (заменить alert)
- Добавить индикаторы загрузки и обратную связь
- Улучшить визуальную иерархию действий

**Фаза 2 (Важно, 2-4 недели):**
- Рефакторинг страницы "Клиенты" (разделение на подстраницы)
- Сохранение состояния фильтров в URL
- Улучшение видимости функциональности (убрать скрытые действия)
- Добавление недостающих фильтров

**Фаза 3 (Желательно, 1-2 месяца):**
- Глобальный поиск
- Улучшение навигации (breadcrumbs, история)
- Экспорт данных
- Расширение кастомизации

### 6.4. Метрики успеха

После внедрения исправлений следует отслеживать:

1. **Время выполнения задач:** Сокращение времени на управление клиентами на 30-40%
2. **Количество ошибок:** Снижение ошибок пользователей на 50%
3. **Удовлетворенность:** Опрос админов о простоте использования
4. **Использование функций:** Увеличение использования скрытых функций на 70-80%

---

**Конец отчета**

