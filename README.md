# 1C Session Manager

Веб-приложение для управления и мониторинга сессий 1С:Предприятие через RAC (Remote Administration Console). Позволяет отслеживать активные сессии, управлять клиентами и их базами данных, автоматически контролировать лимиты подключений и устанавливаться как Windows-сервис.

## Основные возможности

- 📊 **Мониторинг в реальном времени** - отслеживание всех активных сессий 1С на сервере
- 👥 **Управление клиентами** - создание, редактирование и удаление клиентов с настройкой лимитов сессий
- 🗄️ **Управление базами данных** - привязка баз данных к клиентам и контроль их использования
- ⚡ **Автоматическое завершение сессий** - принудительное закрытие подключений при превышении установленных лимитов
- 📈 **Dashboard с аналитикой** - визуализация статистики по сессиям, клиентам и базам данных
- 📝 **Журнал событий** - отслеживание всех системных событий с фильтрацией и сортировкой
- ⚙️ **Настройка подключения** - гибкая конфигурация параметров подключения к кластеру 1С
- 🪟 **Windows-сервис** - возможность установки как системная служба Windows

## Технологический стек

### Frontend
- **React 19** - современный UI-фреймворк
- **TypeScript** - типизированный JavaScript
- **Vite** - быстрый сборщик проектов
- **Tailwind CSS** - utility-first CSS фреймворк
- **Recharts** - библиотека для визуализации данных
- **Lucide React** - набор иконок

### Backend
- **Node.js** - серверная платформа
- **Express** - веб-фреймворк для Node.js

### Интеграции
- **1C RAC** - консоль управления 1С:Предприятие через командную строку

### Развертывание
- **Inno Setup** - создание установочных пакетов для Windows

## Требования

### Для разработки
- **Node.js** (версия 18 или выше)
- **npm** или **yarn**

### Для production
- **Windows Server** (2012 R2 или новее)
- **Node.js** (версия 18 или выше)
- **1С:Предприятие** с установленной и запущенной службой RAS (порт 1545)

## Установка и настройка

### Установка из исходного кода

1. Клонируйте репозиторий:
   ```bash
   git clone <repository-url>
   cd 1CSessionManager
   ```

2. Установите зависимости:
   ```bash
   npm install
   ```

3. Соберите frontend:
   ```bash
   npm run build
   ```

4. Запустите сервер:
   ```bash
   npm run server
   ```

5. Откройте приложение в браузере:
   ```
   http://localhost:3000
   ```

### Установка через setup.exe

Для production-развертывания рекомендуется использовать готовый установочный пакет. Подробные инструкции по сборке и установке см. в [WINDOWS_DEPLOYMENT.md](WINDOWS_DEPLOYMENT.md).

### Настройка подключения к кластеру 1С

После установки приложения необходимо настроить подключение к кластеру 1С:

1. Откройте приложение в браузере (обычно `http://localhost:3000`)
2. Перейдите в раздел **Настройки**
3. Укажите путь к `rac.exe`:
   - Пример: `C:\Program Files\1cv8\8.3.22.1709\bin\rac.exe`
   - Кавычки указывать не нужно
4. Укажите адрес сервера RAS:
   - Формат: `localhost:1545` или `192.168.1.100:1545`
5. При необходимости укажите учетные данные для подключения к кластеру:
   - Имя пользователя кластера
   - Пароль кластера
6. Нажмите **Проверить соединение** для проверки настроек
7. Нажмите **Сохранить настройки**

**Важно:** Убедитесь, что служба RAS запущена на сервере 1С и доступна по указанному адресу.

## Использование

### Dashboard (Главная панель)

Главная страница предоставляет общий обзор состояния системы:
- Общая статистика по сессиям
- Информация о подключенных клиентах
- Статистика по базам данных
- Типы подключений (1CV8, 1CV8C, WebClient)
- Состояние кластера (онлайн/офлайн)
- Метрики сервера (CPU, память)

Данные обновляются автоматически каждые 10 секунд.

### Клиенты

Раздел управления клиентами позволяет:
- **Добавлять новых клиентов** с указанием:
  - Названия клиента
  - Максимального количества сессий
  - Связанных баз данных
- **Редактировать существующих клиентов**
- **Удалять клиентов**
- **Фильтровать и сортировать** список клиентов
- **Просматривать детальную информацию** о клиенте:
  - Текущее количество активных сессий
  - Статус (активный, заблокирован, предупреждение)
  - Список привязанных баз данных

### События

Журнал системных событий отображает:
- Все события системы с уровнем важности:
  - **Info** - информационные сообщения
  - **Warning** - предупреждения
  - **Critical** - критические ошибки
- Фильтрацию по уровню важности
- Поиск по содержимому сообщений
- Сортировку по времени или уровню
- Пагинацию для удобного просмотра
- Автоматическое обновление списка
- Возможность очистки журнала

### Настройки

В разделе настроек можно:
- Настроить путь к RAC
- Указать адрес сервера RAS
- Задать учетные данные для подключения к кластеру
- Настроить интервал проверки сессий (в секундах)
- Включить/выключить режим автоматического завершения сессий (Kill Mode)
- Проверить подключение к кластеру

## Структура проекта

```
1CSessionManager/
├── components/          # React компоненты
│   ├── Sidebar.tsx     # Боковая панель навигации
│   └── StatCard.tsx    # Карточка статистики
├── pages/              # Страницы приложения
│   ├── Dashboard.tsx   # Главная панель
│   ├── Clients.tsx     # Управление клиентами
│   ├── Events.tsx      # Журнал событий
│   └── Settings.tsx    # Настройки
├── services/           # Сервисы
│   └── geminiService.ts # (не используется)
├── server.js           # Express сервер
├── install_service.cjs # Скрипт установки Windows-сервиса
├── uninstall_service.cjs # Скрипт удаления Windows-сервиса
├── setup.iss           # Конфигурация Inno Setup
├── types.ts            # TypeScript типы
├── vite.config.ts      # Конфигурация Vite
└── package.json        # Зависимости проекта
```

## API Endpoints

Приложение предоставляет следующие REST API endpoints:

### Клиенты
- `GET /api/clients` - получить список всех клиентов
- `POST /api/clients` - создать нового клиента
- `PUT /api/clients/:id` - обновить клиента
- `DELETE /api/clients/:id` - удалить клиента

### События
- `GET /api/events` - получить список событий
- `DELETE /api/events` - очистить журнал событий

### Настройки
- `GET /api/settings` - получить текущие настройки
- `POST /api/settings` - сохранить настройки
- `POST /api/test-connection` - проверить подключение к кластеру

### Информационные базы
- `GET /api/infobases` - получить список информационных баз

### Dashboard
- `GET /api/dashboard/stats` - получить общую статистику
- `GET /api/dashboard/top-clients` - получить топ клиентов
- `GET /api/dashboard/warnings` - получить предупреждения

## Безопасность

- Данные приложения (клиенты, настройки, события) хранятся в `C:\ProgramData\1CSessionManager\`
- **Шифрование паролей:** Пароли (пароль кластера 1С и пароль MSSQL) автоматически шифруются с использованием Windows DPAPI (Data Protection API) перед сохранением в файл настроек
  - Шифрование привязано к учетной записи пользователя Windows, что обеспечивает защиту от несанкционированного доступа
  - При первом запуске после обновления существующие незашифрованные пароли будут автоматически зашифрованы при сохранении настроек
  - Зашифрованные пароли могут быть расшифрованы только на том же компьютере и под той же учетной записью, под которой они были зашифрованы
- Приложение должно запускаться с правами администратора для работы с RAC
- Рекомендуется использовать приложение только в защищенной внутренней сети
- Для production-использования рекомендуется настройка HTTPS и дополнительная аутентификация
- **Важно:** Если вы переносите настройки на другой компьютер или под другую учетную запись, потребуется повторно ввести пароли, так как зашифрованные данные привязаны к конкретной учетной записи Windows

## Хранение данных

Все данные приложения сохраняются в следующих файлах в директории `C:\ProgramData\1CSessionManager\`:
- `clients.json` - список клиентов
- `settings.json` - настройки приложения
- `events.json` - журнал событий (последние 100 записей)

## Лицензия

[Укажите лицензию проекта]

## Контакты

[Укажите контактную информацию]

---

**Примечание:** Данное приложение предназначено для использования администраторами серверов 1С:Предприятие. Требуются знания работы с RAC и настройки кластера 1С.
