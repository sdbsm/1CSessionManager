# 1C Session Manager

Веб-приложение для управления и мониторинга сессий 1С:Предприятие через RAC (Remote Administration Console).

Текущая архитектура:
- **Control** (.NET 10): ASP.NET Core API + раздача UI
- **Agent** (.NET 10): Worker Service, который опрашивает RAC и (при необходимости) завершает лишние сессии
- **UI**: React (Vite), собирается и кладётся в `dotnet/src/1CSessionManager.Control/wwwroot/app`

## Основные возможности

- 📊 **Мониторинг в реальном времени** - отслеживание всех активных сессий 1С на сервере
- 👥 **Управление клиентами** - создание, редактирование и удаление клиентов с настройкой лимитов сессий
- 🗄️ **Управление базами данных** - привязка баз данных к клиентам и контроль их использования
- ⚡ **Автоматическое завершение сессий** - принудительное закрытие подключений при превышении установленных лимитов
- 📈 **Dashboard с аналитикой** - визуализация статистики по сессиям, клиентам и базам данных
- 📝 **Журнал событий** - отслеживание всех системных событий с фильтрацией и сортировкой
- ⚙️ **Настройка подключения** - гибкая конфигурация параметров подключения к кластеру 1С
- 🪟 **Windows-сервис** - возможность установки как системная служба Windows

## Технологический стек

### Frontend
- **React 19** - современный UI-фреймворк
- **TypeScript** - типизированный JavaScript
- **Vite** - быстрый сборщик проектов
- **Recharts** - библиотека для визуализации данных
- **Lucide React** - набор иконок

### Backend
- **.NET 10** - платформа
- **ASP.NET Core** - Control (API + static files)
- **Worker Service** - Agent

### Интеграции
- **1C RAC** - консоль управления 1С:Предприятие через командную строку

### Хранилище
- **MSSQL Server** + **EF Core**

## Требования

### Для разработки
- **Node.js** (для сборки UI)
- **.NET SDK 10**

### Для production
- **Windows** (службы Windows)
- **.NET Runtime 10** (или self-contained publish)
- **1С:Предприятие** и служба **RAS** (обычно порт 1545)
- **MSSQL Server**

## Быстрый старт (dev)

1) Установить зависимости UI:

```bash
cd ui
npm install
```

2) Собрать UI (положит файлы в `dotnet/src/1CSessionManager.Control/wwwroot/app`):

```bash
cd ui
npm run build
```

3) Запустить Control и Agent:

```bash
cd dotnet
dotnet run --project src/1CSessionManager.Control
dotnet run --project src/1CSessionManager.Agent
```

4) Открыть UI:

`http://localhost:3000/app/`

## Настройка
Все основные настройки вводятся в UI:
- `http://localhost:3000/app/` → **Settings**

## Документация по .NET части
См. `dotnet/README.md` (миграции, службы Windows, публикация).

## Структура репозитория (актуально)

```
1CSessionManager/
├── ui/                                  # React UI (Vite)
├── dotnet/                              # .NET решение (Control/Agent/Shared + deploy-скрипты)
└── legacy/node/                         # старые Node/installer артефакты (для истории)
```

## Безопасность (актуально)
- **Секреты не хранятся в коде/конфигах**:
  - **SQL login** хранится в **Windows Credential Manager** (DPAPI LocalMachine)
  - **API key** хранится в MSSQL в защищённом виде (DPAPI LocalMachine) и нужен при доступе по IP
